import { ST_BrType } from '../Types.js';

/**
 * http://webapp.docx4java.org/OnlineDemo/ecma376/WordML/br.html
 */
var Break = /** @class */ (function () {
    function Break() {
        /**
         * 目前也只支持这种
         */
        this.type = ST_BrType.textWrapping;
    }
    Break.fromXML = function (word, element) {
        return new Break();
    };
    return Break;
}());

export { Break };
