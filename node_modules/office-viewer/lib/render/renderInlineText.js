'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var tslib_es6 = require('../node_modules/tslib/tslib.es6.js');
var dom = require('../util/dom.js');
var Run = require('../openxml/word/Run.js');
var Bookmark = require('../openxml/word/Bookmark.js');
var Hyperlink = require('../openxml/word/Hyperlink.js');
var renderRun = require('./renderRun.js');
var renderHyperLink = require('./renderHyperLink.js');
var renderBookmark = require('./renderBookmark.js');

/**
 * 渲染 smart tag
 */
function renderInlineText(word, inlineText, parent) {
    var e_1, _a;
    try {
        for (var _b = tslib_es6.__values(inlineText.children), _c = _b.next(); !_c.done; _c = _b.next()) {
            var child = _c.value;
            if (child instanceof Run.Run) {
                dom.appendChild(parent, renderRun["default"](word, child));
            }
            else if (child instanceof Bookmark.BookmarkStart) {
                dom.appendChild(parent, renderBookmark.renderBookmarkStart(word, child));
            }
            else if (child instanceof Hyperlink.Hyperlink) {
                var hyperlink = renderHyperLink.renderHyperLink(word, child);
                dom.appendChild(parent, hyperlink);
            }
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
        }
        finally { if (e_1) throw e_1.error; }
    }
}

exports["default"] = renderInlineText;
