'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var tslib_es6 = require('../node_modules/tslib/tslib.es6.js');
var dom = require('../util/dom.js');
var Run = require('../openxml/word/Run.js');
var renderRun = require('./renderRun.js');

/**
 * 渲染链接
 */
function renderHyperLink(word, hyperlink, paragraph) {
    var e_1, _a;
    var a = dom.createElement('a');
    if (hyperlink.relation) {
        var rel = hyperlink.relation;
        if (rel && rel.targetMode === 'External') {
            a.href = rel.target;
            a.target = '_blank';
        }
    }
    if (hyperlink.anchor) {
        a.href = '#' + hyperlink.anchor;
    }
    try {
        for (var _b = tslib_es6.__values(hyperlink.children), _c = _b.next(); !_c.done; _c = _b.next()) {
            var child = _c.value;
            if (child instanceof Run.Run) {
                var span = renderRun["default"](word, child, paragraph);
                dom.appendChild(a, span);
            }
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
        }
        finally { if (e_1) throw e_1.error; }
    }
    return a;
}

exports.renderHyperLink = renderHyperLink;
