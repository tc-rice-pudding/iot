'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var tslib_es6 = require('../node_modules/tslib/tslib.es6.js');
var dom = require('../util/dom.js');
var renderRun = require('./renderRun.js');

/**
 * 渲染 ruby 标签
 */
function renderRuby(word, ruby) {
    var e_1, _a, e_2, _b;
    var rubyElement = dom.createElement('ruby');
    if (ruby.rubyBase) {
        try {
            for (var _c = tslib_es6.__values(ruby.rubyBase.children), _d = _c.next(); !_d.done; _d = _c.next()) {
                var text = _d.value;
                rubyElement.appendChild(renderRun["default"](word, text));
            }
        }
        catch (e_1_1) { e_1 = { error: e_1_1 }; }
        finally {
            try {
                if (_d && !_d.done && (_a = _c.return)) _a.call(_c);
            }
            finally { if (e_1) throw e_1.error; }
        }
        if (ruby.rt) {
            // rp 是给不支持 ruby 的浏览器显示的
            var rpStart = dom.createElement('rp');
            rpStart.innerText = '(';
            rubyElement.appendChild(rpStart);
            var rtElement = dom.createElement('rt');
            try {
                for (var _e = tslib_es6.__values(ruby.rt.children), _f = _e.next(); !_f.done; _f = _e.next()) {
                    var text = _f.value;
                    rtElement.appendChild(renderRun["default"](word, text));
                }
            }
            catch (e_2_1) { e_2 = { error: e_2_1 }; }
            finally {
                try {
                    if (_f && !_f.done && (_b = _e.return)) _b.call(_e);
                }
                finally { if (e_2) throw e_2.error; }
            }
            rubyElement.appendChild(rtElement);
            var rpEnd = dom.createElement('rp');
            rpEnd.innerText = ')';
            rubyElement.appendChild(rpEnd);
        }
    }
    return rubyElement;
}

exports.renderRuby = renderRuby;
