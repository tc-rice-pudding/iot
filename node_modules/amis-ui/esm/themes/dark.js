/**
 * amis-ui v2.9.0
 * Copyright 2018-2023 fex
 */

import { makeClassnames, theme } from 'amis-core';

var classPrefix = 'dark-';
var classnames = makeClassnames(classPrefix);
theme('dark', {
    classPrefix: classPrefix,
    classnames: classnames,
    renderers: {
        'json': {
            jsonTheme: 'eighties'
        },
        'editor-control': {
            editorTheme: 'vs-dark'
        }
    }
});

export { classPrefix, classnames };
