/**
 * amis-ui v2.9.0
 * Copyright 2018-2023 fex
 */

import { createContext } from 'react';

/**
 * @file Menu
 * @description 导航菜单上下文
 * @author fex
 */
var MenuContext = createContext({
    themeColor: 'light',
    stacked: true,
    mode: 'inline',
    collapsed: false,
    direction: 'ltr',
    prefix: '',
    inlineIndent: 15,
    accordion: false,
    draggable: false
});

export { MenuContext };
