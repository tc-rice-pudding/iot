/**
 * amis-ui v2.9.0
 * Copyright 2018-2023 fex
 */

import { __assign } from 'tslib';
import React__default from 'react';
import InputBox from '../InputBox.js';
import NumberInput from '../NumberInput.js';
import Switch from '../Switch.js';
import { InputJSONSchemaArray } from './Array.js';
import { InputJSONSchemaObject } from './Object.js';

function InputJSONSchemaItem(props) {
    var schema = props.schema;
    if (schema.type === 'object') {
        return React__default.createElement(InputJSONSchemaObject, __assign({}, props));
    }
    else if (schema.type === 'array') {
        return React__default.createElement(InputJSONSchemaArray, __assign({}, props));
    }
    else if (props.renderValue) {
        return props.renderValue(props.value, props.onChange, schema, props);
    }
    else if (schema.type == 'number') {
        return (React__default.createElement(NumberInput, { value: props.value, onChange: props.onChange, placeholder: props.placeholder }));
    }
    else if (schema.type == 'integer') {
        return (React__default.createElement(NumberInput, { value: props.value, onChange: props.onChange, precision: 0, placeholder: props.placeholder }));
    }
    else if (schema.type == 'boolean') {
        return (React__default.createElement(Switch, { value: props.value, onChange: props.onChange, className: "mt-2" }));
    }
    return (React__default.createElement(InputBox, { value: props.value, onChange: props.onChange, placeholder: props.placeholder }));
}

export { InputJSONSchemaItem };
