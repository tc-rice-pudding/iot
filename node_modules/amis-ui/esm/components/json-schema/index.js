/**
 * amis-ui v2.9.0
 * Copyright 2018-2023 fex
 */

import { __assign } from 'tslib';
import React__default from 'react';
import { themeable, localeable } from 'amis-core';
import { InputJSONSchemaItem } from './Item.js';

function InputJSONSchema(props) {
    var schema = props.schema || {
        type: 'object',
        properties: {}
    };
    return React__default.createElement(InputJSONSchemaItem, __assign({}, props, { schema: schema }));
}
var index = themeable(localeable(InputJSONSchema));

export { index as default };
